pipeline{
  agent any
  environment{
    PATH = "/usr/share/maven/bin:$PATH"
  }
  stages{
    stage('Welcome'){
      steps{
        git 'https://github.com/Mudita2806/spring-petclinic'
      }
    }
    stage('Maven Build'){
      steps{
        sh "mvn clean package"
      }
    }
  }
}
